# 📊 Diagrama de Flujo Técnico - Contium

## Flujo Principal del Usuario
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FLUJO DE USUARIO                                   │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
    │  UPLOAD  │ ───▶ │   HASH   │ ───▶ │ REGISTRO │ ───▶ │ VALIDAR  │
    │   📤     │      │   🔐     │      │   ⛓️     │      │    ✅    │
    └──────────┘      └──────────┘      └──────────┘      └──────────┘
         │                 │                 │                 │
         ▼                 ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
    │ Usuario  │      │ Browser  │      │ Blockchain│     │ Contract │
    │ sube PDF │      │ SHA-256  │      │  zkSYS   │      │ +10 pts  │
    └──────────┘      └──────────┘      └──────────┘      └──────────┘
                                                               │
                                                               ▼
                                                         ┌──────────┐
                                                         │  BADGE   │
                                                         │   🏆     │
                                                         └──────────┘
                                                               │
                                                               ▼
                                                         ┌──────────┐
                                                         │ NFT Mint │
                                                         │ ERC-721  │
                                                         └──────────┘
```

## Flujo Técnico Detallado

### 1. Upload de Documento
```
Usuario                    Frontend (Browser)
   │                             │
   │  Selecciona archivo         │
   │ ─────────────────────────▶  │
   │                             │
   │                      ┌──────┴──────┐
   │                      │ FileReader  │
   │                      │ API         │
   │                      └──────┬──────┘
   │                             │
   │  Archivo en memoria         │
   │ ◀─────────────────────────  │
```

### 2. Generación de Hash
```
Frontend                   Web Crypto API
   │                             │
   │  crypto.subtle.digest()     │
   │ ─────────────────────────▶  │
   │                             │
   │                      ┌──────┴──────┐
   │                      │  SHA-256    │
   │                      │  Algorithm  │
   │                      └──────┬──────┘
   │                             │
   │  Hash (bytes32)             │
   │ ◀─────────────────────────  │
   │                             │
   │  0x7a8b9c3d...              │
```

### 3. Registro en Blockchain
```
Frontend               MetaMask              DocumentRegistry
   │                      │                        │
   │  registerDocument()  │                        │
   │ ──────────────────▶  │                        │
   │                      │                        │
   │  Solicita firma      │                        │
   │ ◀──────────────────  │                        │
   │                      │                        │
   │  Usuario confirma    │                        │
   │ ──────────────────▶  │                        │
   │                      │   TX firmada           │
   │                      │ ─────────────────────▶ │
   │                      │                        │
   │                      │                 ┌──────┴──────┐
   │                      │                 │ Almacena:   │
   │                      │                 │ - hash      │
   │                      │                 │ - owner     │
   │                      │                 │ - timestamp │
   │                      │                 └──────┬──────┘
   │                      │                        │
   │                      │   TX receipt           │
   │                      │ ◀───────────────────── │
   │  Confirmación        │                        │
   │ ◀──────────────────  │                        │
```

### 4. Validación de Documento
```
Frontend               MetaMask              DocumentRegistry
   │                      │                        │
   │  validateDocument()  │                        │
   │ ──────────────────▶  │                        │
   │                      │   TX firmada           │
   │                      │ ─────────────────────▶ │
   │                      │                        │
   │                      │                 ┌──────┴──────┐
   │                      │                 │ Verifica:   │
   │                      │                 │ - existe?   │
   │                      │                 │ - es owner? │
   │                      │                 │ - ya valid? │
   │                      │                 └──────┬──────┘
   │                      │                        │
   │                      │                 ┌──────┴──────┐
   │                      │                 │ Actualiza:  │
   │                      │                 │ - validated │
   │                      │                 │ - score +10 │
   │                      │                 └──────┬──────┘
   │                      │                        │
   │  Evento emitido      │                        │
   │ ◀─────────────────────────────────────────────│
```

### 5. Mint Badge NFT
```
Frontend               MetaMask              ContiumBadge         DocumentRegistry
   │                      │                      │                      │
   │  mintBadge()         │                      │                      │
   │ ──────────────────▶  │                      │                      │
   │                      │   TX firmada         │                      │
   │                      │ ───────────────────▶ │                      │
   │                      │                      │                      │
   │                      │                      │  isValidated(hash)?  │
   │                      │                      │ ───────────────────▶ │
   │                      │                      │                      │
   │                      │                      │  true ✅             │
   │                      │                      │ ◀─────────────────── │
   │                      │                      │                      │
   │                      │               ┌──────┴──────┐               │
   │                      │               │ Mint NFT    │               │
   │                      │               │ ERC-721     │               │
   │                      │               └──────┬──────┘               │
   │                      │                      │                      │
   │  NFT recibido 🏆     │                      │                      │
   │ ◀─────────────────────────────────────────  │                      │
```

## Arquitectura de Contratos
```
┌─────────────────────────────────────────────────────────────────┐
│                        zkSYS PoB DevNet                         │
│                        Chain ID: 57042                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────┐    ┌─────────────────────────┐   │
│   │    DocumentRegistry     │    │     ContiumBadge        │   │
│   │  0xd707cc8D...6322      │◀───│   0x912675...4959       │   │
│   ├─────────────────────────┤    ├─────────────────────────┤   │
│   │                         │    │                         │   │
│   │  + registerDocument()   │    │  + mintBadge()          │   │
│   │  + validateDocument()   │    │  + balanceOf()          │   │
│   │  + getDocument()        │    │  + ownerOf()            │   │
│   │  + getScore()           │    │  + tokenURI()           │   │
│   │  + getTopUsers()        │    │                         │   │
│   │  + isValidated()        │    │  ERC-721 Standard       │   │
│   │                         │    │                         │   │
│   └─────────────────────────┘    └─────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Flujo de Datos
```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Usuario │     │ Frontend │     │ MetaMask │     │ Blockchain│
└────┬─────┘     └────┬─────┘     └────┬─────┘     └────┬─────┘
     │                │                │                │
     │  Sube doc      │                │                │
     │───────────────▶│                │                │
     │                │                │                │
     │                │ Genera hash    │                │
     │                │ (local)        │                │
     │                │                │                │
     │  Muestra hash  │                │                │
     │◀───────────────│                │                │
     │                │                │                │
     │  Click Registrar                │                │
     │───────────────▶│                │                │
     │                │                │                │
     │                │ Solicita TX    │                │
     │                │───────────────▶│                │
     │                │                │                │
     │  Popup firma   │                │                │
     │◀───────────────────────────────│                │
     │                │                │                │
     │  Confirma      │                │                │
     │───────────────────────────────▶│                │
     │                │                │                │
     │                │                │  Envía TX      │
     │                │                │───────────────▶│
     │                │                │                │
     │                │                │  TX Hash       │
     │                │                │◀───────────────│
     │                │                │                │
     │  Confirmación  │                │                │
     │◀───────────────────────────────────────────────│
     │                │                │                │
```

---

© 2025 Contium by ChainPort